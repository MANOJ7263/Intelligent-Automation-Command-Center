# ===============================
# Application
# ===============================
spring.application.name=IACC
server.port=8081

# ===============================
# MySQL Database Configuration (PRODUCTION)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/iacc_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=m@noj&7263hi92
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# H2 (Disabled - Using MySQL)
# spring.datasource.url=jdbc:h2:mem:iacc_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# ===============================
# JPA / Hibernate Configuration
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# Logging (Optional but Professional)
# ===============================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration
jwt.secret=IACC_SUPER_SECRET_KEY_ChangeLater
jwt.expiration=86400000

# UiPath Orchestrator
# UiPath Orchestrator (MOCK)
uipath.orchestrator.url=http://localhost:8081/api/mock-orchestrator

# UiPath Auth (MOCK)
uipath.auth.url=http://localhost:8081/api/mock-orchestrator/api/account/authenticate

# UiPath Real (Commented Out)
# uipath.orchestrator.url=https://cloud.uipath.com/hindustaninstituteoftechnologycoimbatore/DefaultTenant/orchestrator_
# uipath.auth.url=https://cloud.uipath.com/identity_/connect/token

# UiPath Configuration (MOCK values)
uipath.folder.id=1
uipath.release.key=mock-release-key
uipath.asset.name=mock-asset
